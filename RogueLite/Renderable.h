#pragma once

// TEXTURE
// Screen X, Y

// RENDER METHOD

class Renderable
{
    unsigned int texture;
    float        x_position, y_position, width, height;
    float        degrees_rotation;
    float        scale;
};